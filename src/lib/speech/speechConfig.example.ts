/**
 * Speech-to-text uses Google Gemini. Configure the key via environment:
 *
 * 1. Copy `.env.example` → `.env` in the project root.
 * 2. Set `GEMINI_API_KEY=...` (Google AI Studio).
 * 3. Restart Metro (`npx react-native start --reset-cache` if the key does not apply).
 *
 * Do not commit `.env`. Prefer a backend proxy for production builds.
 */
