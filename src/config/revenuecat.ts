export const REVENUECAT_API_KEY = 'test_BigavvfozYZsbQJLNkXlWOvyEmO';

/** Must exactly match the entitlement identifier configured in RevenueCat. */
export const PRO_ENTITLEMENT_ID = 'evencal Pro';

/** Product identifiers configured in stores and mirrored in RevenueCat. */
export const REVENUECAT_PRODUCT_IDS = {
  lifetime: 'lifetime',
  yearly: 'yearly',
  monthly: 'monthly',
} as const;

